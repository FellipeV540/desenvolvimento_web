const preco = 30
let resultado = (preco < 40) ? "Livro barato" : "Livro não é barato";
console.log(resultado);

let meuObjeto = {
  propriedade1 : 'um texto',
  propriedade2 : 4002
}

var avore = {
  carvalhoI : 15
}
